# FastAPI & Web Framework
fastapi==0.110.1
uvicorn[standard]==0.27.0
starlette==0.37.2
python-multipart==0.0.22
email-validator==2.3.0 
pandas

# Database
sqlmodel==0.0.37
SQLAlchemy==2.0.48
asyncpg==0.31.0
alembic==1.12.1

# Authentication & Security
bcrypt==4.1.3
python-jose[cryptography]==3.5.0
PyJWT==2.11.0
cryptography==50.0.1
passlib==1.7.4

# Web Push notifications
pywebpush==2.4.0

# Caching
redis==5.0.1
aioredis==2.0.1

# PDF & Rendering
xhtml2pdf==0.2.16
jinja2==3.1.6
pandas
reportlab==4.0.9
Pillow>=11.0
qrcode==8.2

# Environment & Config
python-dotenv==1.2.1
pydantic==2.12.5
pydantic-settings==2.13.1
pydantic-core==2.41.5

# Email
aiosmtplib==5.1.0

# HTTP & Requests
httpx==0.28.1
requests==2.32.5

# Deployment
gunicorn==21.2.0

# Error monitoring (optional — inert until SENTRY_DSN is set)
sentry-sdk[fastapi]==2.66.0

# Utilities
python-dateutil==2.9.0.post0
PyYAML==6.0.3

# Testing (dev only) — the async in-memory SQLite driver every tests/test_*.py
# fixture relies on (sqlite+aiosqlite:///:memory:). Without it those fixtures
# silently never resolve instead of raising, so tests pass for the wrong
# reason rather than actually exercising a database.
pytest==8.4.2
pytest-asyncio==0.22.0
aiosqlite==0.22.1
apscheduler==3.10.4
